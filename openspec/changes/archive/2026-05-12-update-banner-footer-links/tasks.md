## 1. Header Updates

- [x] 1.1 在 `src/components/Header/Header.tsx` 中，將渲染 `<button className="login-btn">{t('login')}</button>` 的程式碼使用註解隱藏。

## 2. Footer Updates

- [x] 2.1 在 `src/components/Footer/Footer.tsx` 中，找到 `footer_catalog` 對應的 `<Link>` 標籤。
- [x] 2.2 將該 `<Link>` 的 `to` 屬性從 `to="/catalog"` 改為 `to="/category"`。
